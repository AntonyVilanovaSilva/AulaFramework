@model IEnumerable<AulaFramework.Models.ANO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ABERTO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ABERTO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ANO1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.ANO1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ANO1 })
        </td>
    </tr>
}

</table>
